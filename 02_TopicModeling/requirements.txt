# Topic-modelling stack (BERTopic + coherence + diversity).
# Install into a dedicated Python 3.12 venv (project convention: .venv312):
#   python3.12 -m venv .venv312
#   .venv312/bin/pip install -r 02_TopicModeling/requirements.txt
#
# NumPy is held at 1.26.x because gensim 4.3.x and the BERTopic dependency
# stack do not yet support NumPy 2.x. The root requirements.txt (.venv,
# notebook / pandas only) intentionally uses numpy 2.x; do not mix the two.
bertopic==0.17.4
sentence-transformers==5.2.3
umap-learn==0.5.11
hdbscan==0.8.41
scikit-learn==1.8.0
gensim==4.3.2
safetensors==0.7.0
numpy==1.26.4
pandas==3.0.0
scipy==1.12.0
